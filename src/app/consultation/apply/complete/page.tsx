import {ApplicationComplete} from '@/components/consultation/ApplicationComplete';

export const metadata = {robots: {index: false, follow: false}, title: '申込受付完了'};

export default function Page() {
  return <ApplicationComplete />;
}
